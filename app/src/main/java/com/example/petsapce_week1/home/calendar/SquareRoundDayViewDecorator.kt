import android.graphics.Color
import android.graphics.Paint
import com.prolificinteractive.materialcalendarview.CalendarDay
import com.prolificinteractive.materialcalendarview.DayViewDecorator
import com.prolificinteractive.materialcalendarview.DayViewFacade
import java.util.*

/*
class RangeDayViewDecorator(private val startDate: CalendarDay, private val endDate: CalendarDay) : DayViewDecorator {

    private val startAndEndDayColor = Paint().apply {
        style = Paint.Style.FILL
        color = Color.parseColor("#EEF5F5")
    }

    private val middleDayColor = Paint().apply {
        style = Paint.Style.FILL
        color = Color.parseColor("#F5F5F5")
    }

  */
/*  override fun shouldDecorate(day: CalendarDay): Boolean {
        return day >= startDate && day <= endDate
    }*//*


    override fun decorate(view: DayViewFacade) {
       */
/* if (view.date == startDate) {
            view.addSpan(RoundBackgroundSpan(startAndEndDayColor))
        } else if (view.date == endDate) {
            view.addSpan(RoundBackgroundSpan(startAndEndDayColor))
        } else {
            view.addSpan(SquareBackgroundSpan(middleDayColor))
        }*//*

    }
}*/
